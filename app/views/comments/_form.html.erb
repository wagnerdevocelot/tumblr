<%= form_for([@post, @post.comments.build]) do |f| %>

    <p>
    <%= f.label :name, "name" %>
    <%= f.text_field :name %>
    </p>

    <p>
        <%= f.label :body, "body" %>
        <%= f.text_area :body %>
    </p>

    <p>
        <%= f.submit %>
    </p>

<% end %>